# Farm Monitor - Environment Variables

# Google Earth Engine Credentials
GEE_CREDENTIALS_PATH=gee-credentials.json
# Alternative: Set GEE_SERVICE_ACCOUNT_EMAIL and GEE_PRIVATE_KEY

# Optional: Email Service (for sending reports)
# SENDGRID_API_KEY=your_sendgrid_api_key
# MAILGUN_API_KEY=your_mailgun_api_key

# Optional: Database (Supabase for storing reports)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_KEY=your_supabase_anon_key

# Server Configuration
PORT=8000
HOST=0.0.0.0
ENVIRONMENT=production

# CORS Settings (Update with your frontend domain in production)
ALLOWED_ORIGINS=*
